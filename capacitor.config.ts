import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asociados.tigo',
  appName: 'AsoTigo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
